environment: local

local {
  services {
    host: "http://localhost"
    pub-booking-app {
      port:9000
      productionRoute = "/"
    }
  }
}

prod {
  services {
    host: "https://pubandkitchenbooking.co.uk"
    productionRoute = "/"
  }
}