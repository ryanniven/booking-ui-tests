environment: local

local {
  services {
    host: "http://localhost"
    name-of-frontend-service {
      port:
      productionRoute = ""
    }
  }
}

qa {
  services {
    host: ${?QA_HOST}
  }
}

externalTest {
  services {
    host: ${?EXTERNAL_TEST_HOST}
  }
}